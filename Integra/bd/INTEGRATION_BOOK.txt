ALTER TABLE ULGU1.INTEGRATION_BOOK
 DROP PRIMARY KEY CASCADE;

DROP TABLE ULGU1.INTEGRATION_BOOK CASCADE CONSTRAINTS;

CREATE TABLE ULGU1.INTEGRATION_BOOK
(
  ID            INTEGER                         NOT NULL,
  ID_SYSTEM     INTEGER                         NOT NULL,
  ID_BOOK       INTEGER                         NOT NULL,
  LOGIN         VARCHAR2(50 CHAR),
  PASSWORD      VARCHAR2(50 CHAR),
  TNS_DATABASE  VARCHAR2(100 CHAR),
  HOST          VARCHAR2(50 CHAR),
  PORT          VARCHAR2(10 CHAR),
  SERVICE_NAME  VARCHAR2(50 CHAR),
  SID           VARCHAR2(50 CHAR),
  DRIVER        VARCHAR2(200 CHAR),
  IS_SEMANTIC   INTEGER
)
TABLESPACE ADMIN_TBL2
RESULT_CACHE (MODE DEFAULT)
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MAXSIZE          UNLIMITED
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING;


CREATE UNIQUE INDEX ULGU1.INTEGRATION_BOOK_PK ON ULGU1.INTEGRATION_BOOK
(ID)
LOGGING
TABLESPACE ADMIN_TBL2
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MAXSIZE          UNLIMITED
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
NOPARALLEL;


ALTER TABLE ULGU1.INTEGRATION_BOOK ADD (
  CONSTRAINT INTEGRATION_BOOK_PK
  PRIMARY KEY
  (ID)
  USING INDEX ULGU1.INTEGRATION_BOOK_PK
  ENABLE VALIDATE);

ALTER TABLE ULGU1.INTEGRATION_BOOK ADD (
  CONSTRAINT INTEGRATION_BOOK_R01 
  FOREIGN KEY (ID_SYSTEM) 
  REFERENCES ULGU1.INTEGRATED_SYSTEMS (ID)
  ENABLE VALIDATE,
  CONSTRAINT INTEGRATION_BOOK_R02 
  FOREIGN KEY (ID_BOOK) 
  REFERENCES ULGU1.BOOKS (ID)
  ENABLE VALIDATE);
