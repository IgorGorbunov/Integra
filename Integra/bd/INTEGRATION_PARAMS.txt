ALTER TABLE ULGU1.INTEGRATION_PARAMS
 DROP PRIMARY KEY CASCADE;

DROP TABLE ULGU1.INTEGRATION_PARAMS CASCADE CONSTRAINTS;

CREATE TABLE ULGU1.INTEGRATION_PARAMS
(
  ID                 INTEGER                    NOT NULL,
  ID_SOURCE_BOOK     INTEGER                    NOT NULL,
  ID_TARGET_BOOK     INTEGER                    NOT NULL,
  TYPE               INTEGER                    NOT NULL,
  ID_SOURCE_ATTR_EQ  INTEGER                    NOT NULL,
  ID_TARGET_ATTR_EQ  INTEGER                    NOT NULL
)
TABLESPACE ADMIN_TBL2
RESULT_CACHE (MODE DEFAULT)
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MAXSIZE          UNLIMITED
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
LOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING;


CREATE UNIQUE INDEX ULGU1.INTEGRATION_PARAMS_PK ON ULGU1.INTEGRATION_PARAMS
(ID)
LOGGING
TABLESPACE ADMIN_TBL2
PCTFREE    10
INITRANS   2
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MAXSIZE          UNLIMITED
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
            FLASH_CACHE      DEFAULT
            CELL_FLASH_CACHE DEFAULT
           )
NOPARALLEL;


ALTER TABLE ULGU1.INTEGRATION_PARAMS ADD (
  CONSTRAINT INTEGRATION_PARAMS_PK
  PRIMARY KEY
  (ID)
  USING INDEX ULGU1.INTEGRATION_PARAMS_PK
  ENABLE VALIDATE);

ALTER TABLE ULGU1.INTEGRATION_PARAMS ADD (
  CONSTRAINT INTEGRATION_PARAMS_R01 
  FOREIGN KEY (ID_SOURCE_BOOK) 
  REFERENCES ULGU1.INTEGRATION_BOOK (ID)
  ENABLE VALIDATE,
  CONSTRAINT INTEGRATION_PARAMS_R02 
  FOREIGN KEY (ID_TARGET_BOOK) 
  REFERENCES ULGU1.INTEGRATION_BOOK (ID)
  ENABLE VALIDATE,
  CONSTRAINT INTEGRATION_PARAMS_R03 
  FOREIGN KEY (ID_SOURCE_ATTR_EQ) 
  REFERENCES ULGU1.INTEGRATION_ATTRIBUTES (ID)
  ENABLE VALIDATE,
  CONSTRAINT INTEGRATION_PARAMS_R04 
  FOREIGN KEY (ID_TARGET_ATTR_EQ) 
  REFERENCES ULGU1.INTEGRATION_ATTRIBUTES (ID)
  ENABLE VALIDATE);
